tasks:
  - name: Dash
    before: |
      pyenv versions
      "yes" | pyenv install 3.11
      pyenv global 3.11
      pyenv version
      pyenv which pip
      pip install --upgrade pip
      pip install pipenv
      python -V
      pipenv shell


vscode:
  extensions:
    - phil294.git-log--graph
    - mhutchie.git-graph
    - phil294.git-log--graph
    - almenon.arepl